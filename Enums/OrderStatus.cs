namespace Inventory_PLM.Enums
{
    
    public enum OrderStatus
    {
        Pending = 0,      // En attente de validation
        Validated = 1,    // Validée
        Shipped = 2,      // Expédiée
        Delivered = 3,    // Livrée
        Cancelled = 4     // Annulée
    }

   
}
